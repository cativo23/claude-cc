node_modules/
coverage/
# dist/ is committed — the plugin system clones the repo directly and needs the built binary
dist/**/*.map
dist/**/*.d.ts
*.tgz
.claude/

# Local plans/specs from superpowers skill
docs/superpowers/

# Git worktrees
.worktrees/

# Internal planning doc — distribution strategy, not for the public repo
/ROADMAP.md
