# Custom Commands

This doc moved — "Custom Commands" is now called **Custom Widgets** (the config key `customCommands` and CLI `lumira custom` still work as permanent aliases, nothing breaks).

See **[docs/custom-widgets.md](custom-widgets.md)** for the current documentation.
